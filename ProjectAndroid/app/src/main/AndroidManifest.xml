<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projectandroid">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.hangdelllaptopMainActivity"></activity>
        <activity android:name=".activity.hangsamsungActivity" />
        <activity android:name=".activity.Account" />
        <activity
            android:name=".activity.RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.loginActivity"
            android:theme="@style/AppTheme.NoActionBar" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".activity.FragmentUpdateActivity"
            tools:ignore="Instantiatable,MissingClass" />
        <activity
            android:name=".activity.FragmentInsertActivity"
            tools:ignore="Instantiatable" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.Map"
            android:label="@string/title_activity_map" />
        <activity
            android:name=".activity.bottomenu"
            android:label="@string/title_activity_bottomenu"
            tools:ignore="MissingClass" />
        <activity android:name=".activity.ThongtindonhangActivity" />
        <activity android:name=".activity.ThongTinKhachHangActivity" />
        <activity android:name=".activity.GioHangActivity" />
        <activity android:name=".activity.ChiTietSanPhamActivity" />
        <activity android:name=".activity.LienHeActivity" />
        <activity android:name=".activity.CuActivity" />
        <activity android:name=".activity.RauActivity" />
        <activity android:name=".activity.DienThoaiActivity" />
        <activity android:name=".activity.MainActivity">


        </activity>

        <activity android:name=".activity.EditAccount" />
        <activity android:name=".activity.AddAccount" />
        <activity android:name=".activity.Contact" />
        <activity android:name=".activity.EditProduct" />
        <activity android:name=".activity.AddProduct" />
        <activity android:name=".activity.ManagerProduct">

        </activity>
        <activity android:name=".activity.ManagerAccount"></activity>
        <activity android:name=".activity.Admin" />
    </application>

</manifest>